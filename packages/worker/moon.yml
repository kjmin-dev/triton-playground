language: python

tasks:
  dev:
    command: uvicorn api.main:app --reload --host 0.0.0.0 --port 8080
    options:
      persistent: true
  lint:
    command: ruff check .
  format:
    command: ruff format .
