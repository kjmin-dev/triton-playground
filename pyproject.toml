[project]
name = "triton-playground"
version = "0.1.0"
requires-python = ">=3.10,<3.11"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=7.0",
    "ruff>=0.4",
]

[tool.ruff]
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
